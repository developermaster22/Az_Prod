  /* --- Estilos generales para las solicitudes --- */
    .col-md-4 {
        background-color: #F8F9FA !important;
        padding: 10px;
    }

    /* --- Estilo común para secciones tipo acordeón --- */
    .accordion-section > .card-header > .button-accordion {
        width: 100%;
        padding: 12px 16px;
        border-radius: 8px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-weight: bold;
        transition: all 0.2s ease;
        font-size: 1rem;
    }

    /* --- Vencidas --- */
    .vencidas-title {
        background-color: #FFEBEE;
        border: 2px solid #D32F2F;
        color: #B71C1C;
    }

    .vencidas-title::before {
        content: "⚠️ ";
        margin-right: 6px;
    }

    /* --- Próximas a Vencer --- */
    .proximas-vencer-title {
        background-color: #FFF3E0;
        border: 2px solid #FFA000;
        color: #E65100;
    }

    .proximas-vencer-title::before {
        content: "⏳ ";
        margin-right: 6px;
    }

    /* --- 'Todas'-- */
    #headingTodas > .button-accordion {
        background-color: #F5F5F5;
        border: 2px solid #BDBDBD;
        color: #424242;
    }

    #headingTodas > .button-accordion::before {
        content: "📋 ";
        margin-right: 6px;
    }

    /* --- Efecto Hover --- */
    .button-accordion:hover {
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        transform: scale(1.02);
        cursor: pointer;
    }

    /* --- Ícono rotativo al expandir --- */
    .button-accordion i {
        transition: transform 0.2s ease;
    }

    .button-accordion[aria-expanded="true"] i {
        transform: rotate(180deg);
    }

    /* --- Estilo tarjeta de detalles --- */
    .details-card {
        background-color: #ffffff;
        border: 1px solid #dee2e6;
        border-radius: 8px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
    }

